# Clerk Configuration
# IMPORTANT: Test keys (pk_test_*) only work in localhost development!
# For production, replace with Live keys (pk_live_*/sk_live_*) from Clerk Dashboard.
# Test keys on a live domain cause "dev-browser-missing" handshake redirects.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
CLERK_SECRET_KEY=your_clerk_secret_key_here
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# Clerk Domain Configuration (for custom FAPI domain)
# Set this to the domain where YOUR APP is hosted, NOT the FAPI domain.
# With live keys, Clerk automatically derives the FAPI domain:
#   addClerkPrefix("yourdomain.com") → "clerk.yourdomain.com"
# Do NOT set NEXT_PUBLIC_CLERK_IS_SATELLITE=true — this app is a PRIMARY app.
# NEXT_PUBLIC_CLERK_IS_SATELLITE is only for multi-domain satellite setups.
NEXT_PUBLIC_CLERK_DOMAIN=yourdomain.com
#
# ⚠️  CUSTOM FAPI DOMAIN DNS REQUIREMENTS:
# 1. Must be a CNAME record pointing to your Clerk FAPI host (e.g. xxx.clerk.accounts.dev)
# 2. Must use "DNS only" (grey cloud ☁️) in Cloudflare — NOT "Proxied" (orange cloud)
# 3. Must NOT point to your own VPS/server IP
#
# If you see "Failed to load Clerk" / ERR_TIMED_OUT on the login page, it means
# the FAPI domain DNS is wrong. Easiest fix: remove the custom domain from Clerk
# Dashboard and delete the DNS record. Clerk will use its default domain instead.

# Prisma & Database Configuration
# Update the DATABASE_URL with your database credentials
DATABASE_URL=postgresql://username:password@localhost:5432/your_database_name

# Uploadthing Configuration
UPLOADTHING_TOKEN='your_uploadthing_token_here'

# Mux Configuration
MUX_TOKEN_ID=your_mux_token_id
MUX_TOKEN_SECRET=your_mux_token_secret

# Stripe Configuration
STRIPE_API_KEY=your_stripe_api_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# Server Port (default: 3000)
# Change this to run on a different port
PORT=3000

# App URL - Must match your server address (no trailing slash)
# IMPORTANT: Must use https:// when behind Cloudflare/Nginx reverse proxy
# Wrong: NEXT_PUBLIC_APP_URL=http://your-domain.com  (causes redirect loops)
# Right: NEXT_PUBLIC_APP_URL=https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_TEACHER_ID=your_teacher_id